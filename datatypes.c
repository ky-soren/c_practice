#include <stdio.h>
#include <stdlib.h>

int main() {
    
    int age = 40;
    double gpa = 40.0;
    char grade = 'A'; //only holds one letter
    char phrase[] = "Hello there"; //use array to hold n characters
    
}